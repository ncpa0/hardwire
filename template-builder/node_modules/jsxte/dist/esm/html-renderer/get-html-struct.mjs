// src/html-renderer/get-html-struct.ts
import { HTMLElementResolver } from "./element-resolver.mjs";
var getHTMLStruct = (element, attributeMap) => {
  if (typeof element.tag === "string") {
    return new HTMLElementResolver(attributeMap).resolveElement(element);
  }
  throw new Error("");
};
export {
  getHTMLStruct
};
