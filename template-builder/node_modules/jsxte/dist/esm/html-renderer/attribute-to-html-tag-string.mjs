// src/html-renderer/attribute-to-html-tag-string.ts
var attributeToHtmlTagString = ([key, value]) => {
  if (value === true) {
    return `${key}`;
  }
  if (value === false || value === null || value === void 0) {
    return "";
  }
  return `${key}="${value.toString().replace(/"/g, "&quot;")}"`;
};
var mapAttributesToHtmlTagString = (attributes) => {
  const results = [];
  for (let i = 0; i < attributes.length; i++) {
    const attribute = attributes[i];
    const html = attributeToHtmlTagString(attribute);
    if (html.length > 0)
      results.push(html);
  }
  return results;
};
export {
  attributeToHtmlTagString,
  mapAttributesToHtmlTagString
};
