// src/string-template-renderer/interpolate.ts
var Interpolate = class {
  /**
   * @internal
   */
  static _isInterpolate(o) {
    const canBeClass = typeof o === "function";
    const isNotNull = o !== null;
    if (!canBeClass || !isNotNull)
      return false;
    const baseName = o._baseName;
    return baseName === this._baseName;
  }
  constructor(_) {
  }
};
Interpolate._baseName = "Interpolate";
var InterpolateTag = class {
  /**
   * @internal
   */
  static _isInterpolateRender(o) {
    const canBeClass = typeof o === "function";
    const isNotNull = o !== null;
    if (!canBeClass || !isNotNull)
      return false;
    const baseName = o._baseName;
    return baseName === this._baseName;
  }
  constructor(_) {
  }
};
InterpolateTag._baseName = "InterpolateTag";
export {
  Interpolate,
  InterpolateTag
};
