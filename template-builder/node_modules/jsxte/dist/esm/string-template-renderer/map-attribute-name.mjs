// src/string-template-renderer/map-attribute-name.ts
var mapAttributeName = (attributeName, map) => {
  if (attributeName in map && map[attributeName]) {
    return map[attributeName];
  }
  return attributeName;
};
export {
  mapAttributeName
};
