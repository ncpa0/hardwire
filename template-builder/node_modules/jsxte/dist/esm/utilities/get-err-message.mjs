// src/utilities/get-err-message.ts
var getErrorMessage = (err) => {
  if (err instanceof Error) {
    return err.message;
  }
  if (typeof err === "string") {
    return err;
  }
  return String(err);
};
export {
  getErrorMessage
};
