"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/html-renderer/attribute-to-html-tag-string.ts
var attribute_to_html_tag_string_exports = {};
__export(attribute_to_html_tag_string_exports, {
  attributeToHtmlTagString: () => attributeToHtmlTagString,
  mapAttributesToHtmlTagString: () => mapAttributesToHtmlTagString
});
module.exports = __toCommonJS(attribute_to_html_tag_string_exports);
var attributeToHtmlTagString = ([key, value]) => {
  if (value === true) {
    return `${key}`;
  }
  if (value === false || value === null || value === void 0) {
    return "";
  }
  return `${key}="${value.toString().replace(/"/g, "&quot;")}"`;
};
var mapAttributesToHtmlTagString = (attributes) => {
  const results = [];
  for (let i = 0; i < attributes.length; i++) {
    const attribute = attributes[i];
    const html = attributeToHtmlTagString(attribute);
    if (html.length > 0)
      results.push(html);
  }
  return results;
};
