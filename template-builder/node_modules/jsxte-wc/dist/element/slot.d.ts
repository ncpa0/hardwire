export declare class SlotAttributeChangeEvent extends CustomEvent<{
    node: WcSlot;
}> {
    constructor(node: WcSlot);
}
export declare class SlotContentChangeEvent extends CustomEvent<{
    node: WcSlot;
}> {
    constructor(node: WcSlot);
}
export declare class WcSlot extends HTMLElement {
    _isWcSlot: boolean;
    static isSlot(elem: Element | Node): elem is WcSlot;
    private _observer;
    emitter: EventTarget;
    constructor();
    private _handleObserverEvent;
    protected shouldEmitAttributeChangeEvent(mutations: MutationRecord[]): boolean;
    protected shouldEmitContentChangeEvent(mutations: MutationRecord[]): boolean;
    protected connectedCallback(): void;
    protected disconnectedCallback(): void;
}
